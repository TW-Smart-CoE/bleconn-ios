import CoreBluetooth
import SwiftUI
import BleConn

struct BleServerView: View {
  @EnvironmentObject private var router: Router

  var body: some View {
    VStack {
    }
    .navigationTitle("BleServer")
  }
}
